import { LandingPage } from "./LandingPage";
import React from "react";

describe("LandingPage", function () {
  it("toggles the document classes when switching between dark and light mode", function () {});
});
